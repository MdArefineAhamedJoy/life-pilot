services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: life-os-ai
    ports:
      - "${APP_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
